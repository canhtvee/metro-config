{
  "name": "metro-config-canhtvee",
  "version": "0.54.1",
  "description": "🚇 Config parser for Metro",
  "main": "src/index.js",
  "files": [
    "src"
  ],
  "repository": {
    "type": "git",
    "url": "git@github.com:canhtvee/metro-config.git"
  },
  "scripts": {
    "prepare-release": "test -d build && rm -rf src.real && mv src src.real && mv build src",
    "cleanup-release": "test ! -e build && mv src build && mv src.real src",
    "publish": "npm publish --access public"
  },
  "license": "MIT",
  "dependencies": {
    "cosmiconfig": "^5.0.5",
    "jest-validate": "^24.7.0",
    "metro": "0.54.1",
    "metro-cache": "0.54.1",
    "metro-core": "0.54.1",
    "pretty-format": "^24.7.0"
  },
  "devDependencies": {
    "strip-ansi": "^4.0.0"
  }
}
